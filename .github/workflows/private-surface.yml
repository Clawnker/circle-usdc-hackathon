name: Private Surface Guard

on:
  pull_request:
  push:
    branches: [main]

jobs:
  private-surface-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enforce private surface policy
        run: bash tests/scripts/check_private_surface.sh
